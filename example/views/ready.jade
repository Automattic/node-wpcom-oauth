
extends layout

block content
  p Simple application to test 
    a(href="https://www.github.com/cloudup/wp-oauth") WPOAuth module.

  h2 Ready to request the access token
  p The code gotten is <em>#{code}</em>. This code allow us get for the access token.

  p.msg
    a(href='/get_token/' + code) Let's get it!
